'use client';

// This file imports the automatic registration helper from next-pwa.
// It must be a client component so the registration runs in the browser.
import 'next-pwa/register';

export default function ServiceWorkerRegister() {
    return null;
}
